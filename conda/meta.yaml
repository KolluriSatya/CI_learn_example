package:
  name: kma
  version: 1.4.9

source:
  url: https://bitbucket.org/genomicepidemiology/kma.git

build:
  number: 2

requirements:
  host:
    - zlib
  build:
    - make
  run:
    - zlib

test:
  commands:
  - kma -h grep KMA >/dev/null